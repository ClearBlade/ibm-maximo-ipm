const maximoReadParams = {
    assetnum: "BU1",
    params: ["assetnum", "serialnum", "assettype", "serialnum", "siteid", "priority", "status"]
    // Set above to your list of params.
    // replace with ["*"] to read ALL params
};
